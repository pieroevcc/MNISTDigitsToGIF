# Core dependencies for the from-scratch video diffusion model.
#
# GPU note: for CUDA (e.g. RTX 4060), install torch/torchvision from the CUDA
# wheel index FIRST, then the rest:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
torch
torchvision
numpy
imageio
